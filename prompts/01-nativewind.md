Read AGENTS.md first and follow it strictly.

Set up NativeWind in this Expo app using the linked v5 documentation for configuration guidance only. The installed versions are `nativewind@5.0.0-preview.4` and `react-native-css@3.0.7` — do not upgrade NativeWind, install a different major/preview version, or swap in a replacement styling library without explicit user approval. Apply the required config, globals.css setup, Babel/Metro changes, TypeScript types, and app entry imports as needed for the installed versions. Do not use outdated setup steps or different version docs.

([Here paste the latest Nativewind documentation](https://www.nativewind.dev/v5/getting-started/installation))

